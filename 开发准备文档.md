# HardwareModularWorkflow 开发准备文档（以硬件为核心）

> 本文档以**硬件**为架构中心重新梳理项目分层。所有功能（工作流、通讯、UI）均围绕硬件抽象层展开，通讯控制器（Plc/Can）作为硬件的底层适配基础设施。

---

## 一、设计核心理念

**HardwareModularWorkflow 是一款以硬件为中心的模块化工作流软件。**

- **硬件是核心**：所有功能（工作流编排、通讯控制、数据持久化、UI 展示）均围绕硬件模型展开
- **通讯控制器是适配器**：Plc 和 Can 控制器是硬件的底层通讯基础设施，负责屏蔽不同厂商的 DLL 差异
- **工作流是组合层**：模块（Module）和流（Flow）是硬件的组合与编排方式，不感知具体通讯细节
- **基本功能覆盖**：暂不考虑各厂商特色功能或私密通讯协议，只统一基础操作（启动、停止、参数读写、状态读取）

---

## 二、项目结构（以硬件为中心重新划分）

### 2.1 项目列表与职责

| 项目 | 命名空间 | 职责定位 | 在架构中的位置 |
|------|---------|---------|--------------|
| `HardwareModularWorkflow.Hardware` | `*.Hardware.Models` / `*.Hardware.Abstractions` | **硬件核心层**：硬件类型系统、命令抽象、状态模型、通讯接口定义 | 中心层，被所有上层依赖 |
| `HardwareModularWorkflow.Controller` | `*.Controller.Plc` / `*.Controller.Can` / `*.Controller.Adapters` | **通讯控制器层**：Plc/Can 控制器实现、厂商 DLL 适配器 | 基础设施层，实现 Hardware 定义的接口 |
| `HardwareModularWorkflow.Workflow` | `*.Workflow.Engine` / `*.Workflow.Models` | **工作流引擎层**：模块定义、流编排、执行顺序控制、同步/异步调度 | 组合层，依赖 Hardware 的模型 |
| `HardwareModularWorkflow.Core` | `*.Core` | **调度编排层**：协调工作流、硬件、控制器、数据持久化，提供统一入口 | 应用层，注入所有下层实现 |
| `HardwareModularWorkflow.Db` | `*.Db.Services` / `*.Db.Entities` | **数据持久化层**：硬件配置、工作流定义、执行日志的存储 | 基础设施层 |
| `HardwareModularWorkflow.Wpf` | `*.Wpf.Views` / `*.Wpf.ViewModels` | **UI 展示层**：工作流编辑、硬件配置、执行监控 | 展示层 |

> **项目调整说明**：原 `MotioncontrolPlc` 扩展为 `Controller` 项目，涵盖 **Plc 和 Can** 两种通讯控制器。

### 2.2 以硬件为中心的分层架构

```
┌─────────────────────────────────────────────────────────────┐
│  Presentation Layer                                         │
│  HardwareModularWorkflow.Wpf                               │
│  └── 工作流编辑器、硬件配置面板、执行监控界面                │
├─────────────────────────────────────────────────────────────┤
│  Application / Orchestration Layer                          │
│  HardwareModularWorkflow.Core                                │
│  └── 工作流加载、执行调度、事件总线、依赖注入                 │
├─────────────────────────────────────────────────────────────┤
│  Workflow Engine Layer                                      │
│  HardwareModularWorkflow.Workflow                            │
│  └── 模块组合、流编排、同步/异步执行引擎                     │
├─────────────────────────────────────────────────────────────┤
│  Hardware Abstraction Layer (核心)                          │
│  HardwareModularWorkflow.Hardware                            │
│  ├── 硬件类型系统（电机、制温、制冷、自定义）                  │
│  ├── 硬件命令抽象（启动、停止、参数设置、状态读取）            │
│  ├── 硬件状态模型                                           │
│  └── 通讯控制器接口定义（IPlcController / ICanController）  │
├─────────────────────────────────────────────────────────────┤
│  Communication / Controller Layer (基础设施)                  │
│  HardwareModularWorkflow.Controller                          │
│  ├── PlcController：PLC 通讯实现                           │
│  ├── CanController：CAN 通讯实现                           │
│  ├── 厂商适配器：统一不同厂商 DLL 的接口差异                  │
│  └── 命令转换：将硬件命令翻译为控制器原生指令                  │
├─────────────────────────────────────────────────────────────┤
│  Data Persistence Layer                                     │
│  HardwareModularWorkflow.Db                                  │
│  └── 硬件配置、工作流定义、执行记录                          │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 依赖关系（箭头方向 = 依赖/引用）

```
Wpf
  ↓
Core
  ↓ ├→ Workflow
  ↓ ├→ Hardware
  ↓ ├→ Controller
  ↓ └→ Db

Workflow
  ↓
Hardware

Controller
  ↓
Hardware（实现 Hardware 定义的 IController 接口）

Db
  （独立，或 Core 做模型映射）
```

**关键原则**：
- **Hardware** 项目处于中心，**不依赖任何其他项目**（只定义抽象和模型）
- **Controller** 实现 Hardware 定义的接口，屏蔽厂商差异
- **Workflow** 只操作 Hardware 模型，不感知 Plc/Can 细节
- **Core** 负责注入具体 Controller 实现，协调全流程

---

## 三、技术栈

### 3.1 跨项目通用技术栈

| 技术/库 | 版本/范围 | 用途 | 应用项目 |
|---------|----------|------|---------|
| `.NET 8 / .NET Framework 4.8` | 运行时 | 目标框架 | 全部 |
| `Microsoft.Extensions.DependencyInjection` | NuGet | 依赖注入容器、服务注册与解析 | Core, Wpf |
| `System.Threading.Channels` | 内置 | 工作流调度队列（`Channel<WorkItem>`） | Core, Workflow |
| `System.Text.Json` | 内置 | 自定义硬件 JSON Schema 序列化/反序列化 | Hardware, Db |
| `Entity Framework Core` / `Dapper` | NuGet（待定） | 数据持久化 | Db |

### 3.2 WPF 专用技术栈

| 技术/库 | 用途 | 应用范围 |
|---------|------|---------|
| `CommunityToolkit.Mvvm` | MVVM 框架：源生成器自动属性、命令、消息传递 | Wpf 项目全部 ViewModel |
| `Antelcat.I18N.WPF` | 国际化/多语言支持：动态语言切换、XAML 绑定翻译 | Wpf 全部 XAML 界面 |
| `MahApps.Metro` | WPF 现代化控件库：窗体样式、按钮、对话框、进度条 | Wpf 全部 Views |
| `MaterialDesignThemes.MahApps` | Material Design + MahApps 风格融合：图标、卡片、浮动按钮、过渡动画 | Wpf 全部 Views，与 MahApps 配合使用 |

### 3.3 技术栈使用约定

| 层级 | 技术约束 |
|------|---------|
| **DI 注册** | Core 项目使用 `Microsoft.Extensions.DependencyInjection` 的 `ServiceCollection` 注册所有服务；Wpf 项目在 `App.xaml.cs` 中构建 `ServiceProvider` 并注入 ViewModel |
| **MVVM** | 所有 ViewModel 继承 `CommunityToolkit.Mvvm` 的 `ObservableObject` / `ObservableRecipient`；命令使用 `[RelayCommand]` 源生成器；属性使用 `[ObservableProperty]` |
| **国际化** | 所有可见字符串通过 `Antelcat.I18N.WPF` 的 `{i18n:I18n Key=xxx}` 绑定；语言资源文件按功能分文件夹存放（`Resources/Strings/zh-CN.xaml`、`en-US.xaml`） |
| **UI 风格** | 全部窗体继承 `MahApps.Metro` 的 `MetroWindow`；MaterialDesign 提供图标和控件增强；自定义控件在 `*.Wpf.Controls` 命名空间下 |
| **异步绑定** | ViewModel 中异步命令用 `AsyncRelayCommand`；UI 绑定结果用 `TaskNotifier` 或 `IsLoading` 状态属性控制界面反馈 |

---

## 四、硬件核心层（Hardware）详细设计

### 3.1 硬件类型系统

硬件是核心元素，类型决定命令方式和属性集。

| 硬件类型 | 基础属性 | 命令集 |
|---------|---------|--------|
| **电机** | 编码器值、位置、速度、扭力、加速度、**HasEncoder（是否带编码器）** | 移动、回零、速度设置、扭力设置、停止 |
| **制温** | 目标温度、当前温度、加热功率 | 设置温度、启动加热、停止加热 |
| **制冷** | 目标温度、当前温度、制冷功率 | 设置温度、启动制冷、停止制冷 |
| **自定义** | 通过 JSON Schema 动态定义 | 动态生成命令集 |

> **关于电机编码器**：上位机应当区分带编码/不带编码的电机。带编码器时，上位机可读取反馈做闭环监控（位置确认、回零校准、完成检测）；不带编码器时，上位机只做开环命令下发。差异通过 `HasEncoder` 参数体现，不单独拆类型。

### 3.2 硬件模型结构

```
Hardware (硬件基类)
├── Id: long
├── Name: string
├── Alias: string
├── Note: string
├── Port: string          // 端口/通道标识
├── Address: string       // 设备地址
├── Type: HardwareType    // 电机 / 制温 / 制冷 / 自定义
├── State: HardwareState  // 空闲 / 运行 / 错误 / 离线
├── Parameters: Dictionary<string, object>  // 类型特定参数
└── CustomSchema: JsonSchema  // 自定义类型时使用
```

### 3.3 硬件命令抽象

**Hardware 层定义通用命令接口，与具体控制器无关。**

| 命令类型 | 说明 | 是否异步 |
|---------|------|---------|
| `Start` | 启动硬件 | 是 |
| `Stop` | 停止硬件 | 是 |
| `Reset` | 复位硬件 | 是 |
| `SetParameter` | 设置参数（速度、温度等） | 是 |
| `GetState` | 读取当前状态 | 否（快速） |
| `Execute` | 执行自定义命令（自定义硬件用） | 是 |

```csharp
// 伪代码示例
public interface IHardwareCommand
{
    string CommandName { get; }
    Dictionary<string, object> Parameters { get; }
    bool IsAsync { get; }
    TimeSpan? Timeout { get; }
}

public interface IHardwareDriver
{
    Task<CommandResult> ExecuteAsync(IHardwareCommand command, CancellationToken ct);
    Task<HardwareState> GetStateAsync(CancellationToken ct);
}
```

### 3.4 通讯控制器接口定义

**Hardware 层只定义接口，具体实现由 Controller 层提供。**

```csharp
// 硬件层定义的通讯控制器接口
public interface IPlcController : IController { }
public interface ICanController : IController { }

public interface IController
{
    string ControllerType { get; }           // "Plc" / "Can"
    string VendorName { get; }               // 厂商名称
    bool IsConnected { get; }
    Task ConnectAsync(CancellationToken ct);
    Task DisconnectAsync(CancellationToken ct);
    Task<byte[]> SendCommandAsync(byte[] data, CancellationToken ct);
    Task<T> ReadRegisterAsync<T>(string address, CancellationToken ct);
    Task WriteRegisterAsync<T>(string address, T value, CancellationToken ct);
}
```

---

## 五、通讯控制器层（Controller）详细设计

### 4.1 职责定位

**Controller 层是 Hardware 的底层基础设施，负责：**
1. 封装 Plc 和 Can 的底层通讯协议
2. 适配不同厂商的 DLL 调用方式（方法名不同但功能相同）
3. 将硬件命令翻译为控制器原生指令
4. 提供统一接口，屏蔽厂商差异

### 4.2 厂商适配器设计

**核心原则**：统一基础功能，不考虑厂商特色或私密协议。

```
Controller Layer
├── IPlcController（Hardware 层定义）
│   └── PlcControllerBase
│       ├── PlcVendorAAdapter（厂商A DLL 适配）
│       ├── PlcVendorBAdapter（厂商B DLL 适配）
│       └── ...
├── ICanController（Hardware 层定义）
│   └── CanControllerBase
│       ├── CanVendorXAdapter（厂商X DLL 适配）
│       └── ...
└── ControllerFactory（根据配置创建具体控制器）
```

| 适配器职责 | 说明 |
|-----------|------|
| **DLL 加载** | 动态加载厂商提供的 DLL |
| **方法映射** | 将统一接口方法映射到厂商 DLL 的具体方法名 |
| **参数转换** | 将通用参数格式转换为厂商原生格式 |
| **结果封装** | 将厂商返回结果封装为统一格式 |

### 4.3 命令映射示例

以电机"移动"命令为例：

```
Hardware 层命令：MoveTo(position: 100, speed: 50)
         ↓
Controller 层翻译：
    PlcVendorAAdapter → MoveAbsolute(100, 50)
    PlcVendorBAdapter -> SetPosition(100); SetSpeed(50); Start()
    CanVendorXAdapter -> SendCanFrame(0x01, [0x64, 0x32])
```

---

## 六、工作流引擎层（Workflow）详细设计

### 5.1 层次结构（以硬件为最小原子）

```
流 (Flow) → 模块 (Module) → 硬件实例 (Hardware Instance) → 命令 (Command)
```

| 层级 | 说明 | 与硬件的关系 |
|------|------|------------|
| **流 (Flow)** | 由多个模块组合，可独立命名 | 不直接引用硬件，只组合模块 |
| **模块 (Module)** | 工作流最小单元，由多个硬件组合 | 引用硬件实例，定义执行顺序 |
| **硬件实例** | 硬件类型的具体配置 + 控制器绑定 | 指向 Hardware 层的模型 |
| **命令** | 对硬件实例的操作 | 调用 Hardware 层定义的命令 |

### 5.2 模块定义

```csharp
public class Module
{
    public long Id { get; set; }
    public string Name { get; set; }
    public string Alias { get; set; }
    public string Note { get; set; }
    
    // 硬件执行顺序（串行 / 并行）
    public List<HardwareStep> HardwareSteps { get; set; }
    
    // 事件
    public EventDefinition CoreEvent { get; set; }
    public EventDefinition NotifyEvent { get; set; }
}

public class HardwareStep
{
    public Hardware Hardware { get; set; }  // 引用 Hardware 层的模型
    public IHardwareCommand Command { get; set; }
    public ExecutionMode Mode { get; set; }  // Sequential / Parallel
    public int Order { get; set; }
}
```

### 5.3 执行模式（基于问题 5 决策更新）

| 模式 | 说明 | 实现方式 |
|------|------|---------|
| **模块内串行** | 硬件按顺序执行 | 一个 `Task` 内按顺序 `await` 每个硬件命令 |
| **模块内并行** | 硬件同时执行 | 一个 `Task` 内 `Task.WhenAll` 多个硬件命令 |
| **流级串行** | 模块按顺序执行 | 完成一个模块的 `Task` 后，再从队列取下一个模块 |
| **流级并行** | 多个模块同时执行 | 多个模块 `Task` 同时竞争 Semaphore 槽位 |
| **全局调度** | 整个系统最多 50 个并发工作流/模块 | `SemaphoreSlim(50)` + `Channel` 队列 |

**执行引擎核心机制**：

```csharp
// Core 层调度器伪代码
public class WorkflowScheduler
{
    private readonly SemaphoreSlim _concurrencyLimit = new(50);
    private readonly Channel<WorkItem> _workQueue;
    
    public async Task RunAsync(CancellationToken ct)
    {
        await foreach (var item in _workQueue.Reader.ReadAllAsync(ct))
        {
            await _concurrencyLimit.WaitAsync(ct);
            _ = ExecuteAsync(item, ct)
                .ContinueWith(_ => _concurrencyLimit.Release(), TaskScheduler.Default);
        }
    }
}
```

- 取消：通过 `CancellationToken` 终止整个调度器，未开始的任务取消，已下发的硬件命令由 Controller 层尝试中断
- 嵌套：工作流引用其他工作流时，将子工作流拆分为独立 `WorkItem` 加入队列，由调度器统一分配 Semaphore 槽位，不做递归调用
- 不做静态嵌套层级限制，运行时通过执行路径 + CancellationToken 防止无限循环

---

## 七、数据持久化层（Db）设计

### 6.1 存储内容

| 数据类别 | 内容 | 存储方式建议 |
|---------|------|------------|
| **硬件模板** | 硬件类型定义（电机、制温等固定类型） | 数据库存表 |
| **硬件实例配置** | 具体硬件的端口、地址、参数、绑定的控制器 | 数据库存表 |
| **自定义硬件 Schema** | 自定义硬件的 JSON 结构定义 | 数据库 JSON 字段或独立文件 |
| **模块定义** | 模块包含的硬件、执行顺序 | 数据库存表 |
| **流定义** | 流包含的模块、模块间依赖 | 数据库存表 |
| **执行日志** | 命令执行结果、耗时、错误信息 | 数据库存表 |
| **控制器配置** | Plc/Can 控制器的连接参数、厂商 DLL 路径 | 数据库存表 + 配置文件 |

### 6.2 自定义硬件存储方案（推荐）

**推荐：混合存储（JSON 存库）**

```sql
-- 硬件定义表
CREATE TABLE HardwareDefinitions (
    Id BIGINT PRIMARY KEY,
    Name NVARCHAR(255),
    Type NVARCHAR(50),        -- 'Motor' / 'Temperature' / 'Cooling' / 'Custom'
    CustomSchema NVARCHAR(MAX), -- 自定义类型时存储 JSON Schema
    Parameters NVARCHAR(MAX)  -- 通用参数 JSON
);
```

**理由**：
- 固定类型（电机、制温）用标准字段，可查询、可索引
- 自定义类型用 JSON 存储 Schema，灵活扩展
- 运行时由 Hardware 层解析 JSON 生成动态命令集

---

## 八、待解决问题清单（以硬件为中心重新梳理）

### ❓ 问题 1：硬件命令如何映射到控制器指令？ ✅ 已决策

**决策**：采用推荐方案 —— **Hardware 层发语义命令**，**Controller 层完全翻译**。Hardware 只关心"做什么"，Controller 关心"怎么做"。

| 层面 | 职责 |
|------|------|
| **Hardware 层** | 定义语义命令（如 `MoveTo(100)`、`SetSpeed(50)`） |
| **Controller 层** | 将语义命令翻译为厂商原生指令（Plc 寄存器操作 / Can 帧下发） |
| **厂商适配器** | 具体方法名映射、参数格式转换、结果封装 |

---

**描述**：Hardware 层定义通用命令（如 `MoveTo`），但不同控制器（Plc/Can）和不同厂商的翻译方式不同。

| 需决策点 | 选项 |
|---------|------|
| 映射位置 | A. Hardware 层知道如何映射到控制器类型；B. Controller 层完全负责翻译，Hardware 只发通用命令 |
| 厂商差异 | 基础功能统一，但参数格式不同（如 Plc 用地址+寄存器，Can 用帧 ID+数据）。翻译逻辑放在适配器还是硬件驱动？ |
| 推荐方案 | **Hardware 层发语义命令**（如 `MoveTo(100)`），**Controller 层完全翻译**。Hardware 只关心"做什么"，Controller 关心"怎么做"。 |

### ❓ 问题 2：电机驱动方式如何配置？ ✅ 已决策

**决策**：上位机**区分带编码/不带编码的电机**，通过参数化方式体现，不拆分为独立类型。

| 层面 | 职责 |
|------|------|
| **Hardware 层** | `MotorHardware` 增加 `HasEncoder: bool` 属性；带编码器时启用反馈相关功能（位置确认、回零检测、完成检测），无编码器时跳过反馈检测，只做开环命令下发 |
| **Controller 层** | 根据 `HasEncoder` 决定是否读取编码器寄存器 / Can 反馈帧 |
| **配置位置** | 添加电机硬件实例时，选择：① 通讯通道（Plc/Can）② 控制器厂商 ③ 是否带编码器 ④ 驱动参数（地址/端口/帧 ID） |
| **运动方式** | 由硬件命令定义（点位运动、速度模式、力矩模式），在 Workflow 层配置 |

> **说明**：在嵌入式层面，带编码与不带编码的电机驱动逻辑差异很大；但在上位机层面，差异主要体现在"是否读取反馈确认"，用参数化方式更灵活，且避免类型爆炸。

**描述**：电机需要驱动器，驱动方式由谁决定？

| 层面 | 职责 |
|------|------|
| **Hardware 层** | 定义电机类型和基础命令（移动、回零、速度设置） |
| **Controller 层** | 选择 Plc 或 Can 控制器，配置厂商 DLL |
| **配置位置** | 添加硬件实例时，选择：① 通讯通道（Plc/Can）② 控制器厂商 ③ 驱动参数（地址/端口/帧 ID） |
| **运动方式** | 由硬件命令定义（点位运动、速度模式、力矩模式），在 Workflow 层配置 |

### ❓ 问题 3：异步执行与硬件同步特性冲突 ✅ 已决策

**决策**：四层协作策略，全部采纳。

| 层面 | 处理策略 |
|------|---------|
| **Controller 层** | 同步 DLL 调用用 `Task.Run` 包装，并用 `SemaphoreSlim` 限制同一控制器实例的并发数（建议单控制器 1 个并发） |
| **Hardware 层** | 命令定义 `IsAsync` 标记；`GetState` 等快速操作为同步，`MoveTo` 等耗时操作为异步 |
| **Workflow 层** | 模块内：标记为同步的硬件强制串行；标记为异步的硬件可并行。模块间：支持同步等待和异步并行两种模式 |
| **Core 层** | 资源锁管理：同一控制器/端口上的硬件命令串行化；不同控制器可并行 |

---

**描述**：Workflow 层支持异步并行，但底层硬件/控制器可能是同步阻塞的。

| 层面 | 处理策略 |
|------|---------|
| **Controller 层** | 尽可能将同步 DLL 调用包装为异步（Task.Run + 信号量限制并发） |
| **Hardware 层** | 定义命令是否支持异步的标记（`IsAsync`） |
| **Workflow 层** | 对标记为同步的硬件，用串行执行；异步的硬件可并行 |
| **资源冲突** | 同一控制器/端口上的硬件强制串行，由 Core 层做资源锁管理 |

### ❓ 问题 4：自定义硬件类型扩展机制 ✅ 已决策

**决策**：第一阶段采用 **JSON Schema + 命令模板**；插件 DLL 机制留作第二阶段扩展。

| 阶段 | 方案 | 说明 |
|------|------|------|
| 第一阶段 | JSON Schema | 用户定义 JSON Schema 描述新硬件的属性集和命令模板，Hardware 层运行时解析并生成动态命令集 |
| 第二阶段（预留） | 插件 DLL | 需要复杂计算或特殊协议时，动态加载实现 `IHardwareDriver` 的 DLL |

---

**描述**：如何让用户添加新的硬件类型而不修改代码？

| 方案 | 说明 |
|------|------|
| **JSON Schema** | 用户定义 JSON Schema 描述新硬件的属性和命令模板，运行时解析 |
| **插件 DLL** | 用户编写实现 `IHardwareDriver` 的 DLL，动态加载 |
| **推荐** | 第一阶段用 JSON Schema + 命令模板；第二阶段考虑插件机制 |

### ❓ 问题 5：工作流循环嵌套与执行机制 ✅ 已决策

**决策**：
- 嵌套层级**不做限制**
- 执行机制改为**异步任务集合**（非线程集合），上限 **50 个并发任务**
- 使用 `CancellationToken` 终结整个任务集合

| 设计点 | 说明 |
|--------|------|
| 并发模型 | 每个工作流（或模块）作为独立 `Task` 加入并发集合，不是 Thread（避免线程资源耗尽） |
| 并发上限 | `SemaphoreSlim(50)` 限制同时执行的工作流数量，完成一个释放一个槽位，再加载下一个 |
| 取消机制 | 外部通过 `CancellationTokenSource.Cancel()` 触发取消，未开始的任务直接取消，已下发的硬件命令由 Controller 层尝试中断（取决于硬件/控制器是否支持中断） |
| 嵌套实现 | 工作流引用其他工作流时，用**队列/栈**调度而非递归调用，避免栈溢出；Workflow 层维护一个待执行队列，Core 层调度器从队列取任务分配 Semaphore 槽位 |
| 死循环检测 | 不做静态循环检测（因为嵌套无限制），但运行时记录执行路径；若同一工作流在路径中重复出现，由 CancellationToken 终结或用户手动停止 |

**伪代码示例**：
```csharp
public class WorkflowScheduler
{
    private readonly SemaphoreSlim _semaphore = new(50); // 并发上限
    private readonly Channel<WorkflowTask> _queue;        // 待执行队列
    
    public async Task RunAsync(CancellationToken ct)
    {
        var running = new List<Task>();
        
        await foreach (var task in _queue.Reader.ReadAllAsync(ct))
        {
            await _semaphore.WaitAsync(ct);
            var t = ExecuteWorkflowAsync(task, ct).ContinueWith(_ => _semaphore.Release());
            running.Add(t);
        }
        
        await Task.WhenAll(running); // 等待全部完成或取消
    }
}
```

> ⚠️ **注意**：CancellationToken 取消的是**调度层面**（不再加载新工作流），但已下发的硬件命令（如电机移动中）能否中断，取决于 Controller 层是否支持发送"急停/中断"指令。建议在 Hardware 层定义 `IStoppable` 接口，Controller 层尽量实现中断。

**描述**：工作流出现循环（如 A → B → A）时如何处理？

| 需决策点 | 建议 |
|---------|------|
| 循环条件 | ① 固定次数循环；② 条件循环（状态满足退出）；③ 无限循环（需手动中断） |
| 嵌套层级 | 建议限制最大 10 层嵌套，防止栈溢出 |
| 死循环检测 | Core 层维护执行图，检测循环依赖；Workflow 层运行时检查循环引用 |
| 循环引用 | 禁止工作流自引用（A → A），但允许跨工作流引用（A → B → A）并做循环检测 |

### ❓ 问题 6：控制器配置与硬件实例的绑定 ✅ 已决策

**决策**：支持**多个 PLC/Can 控制器**，在**添加流/模块时绑定**，配置信息**存入数据库**。

| 设计点 | 说明 |
|--------|------|
| **绑定时机** | 添加/编辑流或模块时，为每个硬件实例选择要绑定的控制器（下拉列表选择已配置的控制器） |
| **数据库结构** | `Controllers` 表存储所有控制器配置（Id、类型 Plc/Can、厂商、连接参数、DLL 路径）；`HardwareInstances` 表增加 `ControllerId` 外键 |
| **运行时注入** | Core 层启动时，从 Db 加载所有控制器配置，通过 `ControllerFactory` 创建控制器实例并缓存；执行工作流时，根据硬件实例的 `ControllerId` 获取对应控制器 |
| **多控制器并行** | 绑定到不同控制器的硬件可并行执行；绑定到同一控制器的硬件由 Core 层资源锁串行化（或按控制器自身的并发能力管理） |

---

**描述**：一个硬件实例如何绑定到具体的 Plc/Can 控制器？

| 层面 | 设计 |
|------|------|
| **Hardware 层** | 硬件实例包含 `ControllerType`（Plc/Can）和 `ControllerId` 引用 |
| **Db 层** | 控制器配置单独存表（连接参数、DLL 路径），硬件实例通过外键关联 |
| **Core 层** | 启动时根据硬件实例的 ControllerId 从 ControllerFactory 获取具体控制器，注入到 HardwareDriver |

---

## 九、开发规范建议

### 8.1 命名规范

| 层级 | 命名空间 | 类名后缀 |
|------|---------|---------|
| 硬件模型 | `*.Hardware.Models` | 无后缀（如 `MotorHardware`） |
| 硬件抽象 | `*.Hardware.Abstractions` | `Ixxx` / `Basexxx` |
| 控制器 | `*.Controller.Plc` / `*.Controller.Can` | `Controller` / `Adapter` |
| 工作流 | `*.Workflow.Engine` | `Engine` / `Scheduler` |
| 视图 | `*.Wpf.Views` | `Page` / `View` / `Window` |
| 视图模型 | `*.Wpf.ViewModels` | `ViewModel` |

### 8.2 项目引用原则（严禁循环依赖）

```
允许：
  Wpf → Core
  Core → Workflow, Hardware, Controller, Db
  Workflow → Hardware
  Controller → Hardware

禁止：
  Hardware → Controller（Hardware 只定义接口，不依赖实现）
  Hardware → Workflow
  Controller → Workflow
  Db → 任何上层项目（Db 保持独立，Core 做映射）
```

### 8.3 异步规范

- 所有控制器通讯操作（DLL 调用、端口读写）必须用 `async/await` 包装
- 同步 DLL 用 `Task.Run` + `SemaphoreSlim` 限制并发
- 硬件命令返回结果必须包含：`Success` / `ErrorCode` / `Duration` / `ResultData`
- 线程安全：同一控制器实例的命令队列化执行（串行），不同控制器可并行

---

## 十、下一步行动（基于已决策方案推进）

1. **定义 Hardware 核心接口**：
   - `IHardware` / `IHardwareCommand` / `IHardwareDriver` / `IController` / `IPlcController` / `ICanController`
   - 电机模型增加 `HasEncoder` 属性

2. **实现基础硬件类型**：
   - 电机（带编码/不带编码参数化）、制温、制冷的模型和命令定义

3. **搭建 Controller 适配层**：
   - 选择一个 Plc 厂商和一个 Can 厂商实现适配器原型
   - 验证 `MoveTo` 语义命令 → 厂商原生指令的翻译链路

4. **实现工作流调度引擎**：
   - `Channel` + `SemaphoreSlim(50)` 的异步任务调度器
   - 支持 CancellationToken 取消
   - 嵌套工作流队列化调度（非递归）

5. **定义数据库结构**：
   - `Controllers` 表（多控制器支持）
   - `HardwareDefinitions` / `HardwareInstances` 表（含 `ControllerId` 外键）
   - `Modules` / `Flows` / `ExecutionLogs` 表

6. **打通 Hardware → Controller → 物理设备 链路**：
   - 验证硬件命令经 Controller 适配器到达物理设备的完整流程

---

> 文档版本：以硬件为中心的架构设计
> 维护者：开发团队
