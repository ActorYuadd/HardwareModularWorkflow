<Window x:Class="HardwareModularWorkflow.Wpf.Views.NewMainWindowExampleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HardwareModularWorkflow.Wpf.Views"
        mc:Ignorable="d"
        Title="NewMainWindowExampleView" Height="450" Width="800">
    <!--
新想法-功能需求 暂定，先进行记录讨论。新功能模块开发说明

对于硬件工作流中 会涉及到 运动控制系统的核心（轨迹规划，多轴同步，其他）

对于 轨迹规划 的目的是什么？？ 我的思考是 计算出轨迹 ”通过合理运动方式“ 运行 讲究平滑，可控，稳定
对于 轨迹规划 的作用？？ 是为了 让多轴同步运行更流畅，耗时更短，稳定
轨迹规划如何实现？如何传递 条件，边界，返回结果？？

对于 多轴同步
多轴的运动导致轨迹是否与预测中一致，还是有变化？
实现方式，随着时间发生变化？运行的极限？每个轴承受的扭矩？

归根到我所做的软件中能怎样提供相对功能？
在 模块类型-电机 中添加 什么？？ 例如
在 工作流中可以添加什么？？ 例如 协同【选择 其他工作流ID，进行组合 成轨迹规划的一部分要点】

我的软件 应该只有 工作流 ，模块
有添加模块的类型。就行

一个 TabInit 【工作流1】 【工作流2】【+】可以重命名，关闭/保存/打开。通过 侧栏{历史、查找、分组}以便打开工作流内容

页面要修改修改参考 NewMainWindowExampleView 完全展开功能示例

    -->


    <!-- 顶部 菜单栏内容（系统、模块、扩展、调试？、搜索）-->
    <!-- 系统 (数据库, 配置, )
            数据库 (类型,连接,共享)
            配置 (日志内容,还有没考虑到有什么)
    -->
    <!-- 模块 (添加模块, 选择模块, 添加模块类型) 可以参照现在的 -->
    <!-- 扩展 描述:提供模块类型的扩展功能
            添加模块驱动文件 (dll/其他编程文件)  不能将全部文件放在同一层中，需要按类型和特性分，类型名称取文件夹名称命名
            添加模块类别类型
            还没想好还有什么内容
    -->
    <!-- 调试 描述:提供调试功能
            运行工作流 (VS中的运行按钮差不多,选择工作流执行...但没有页面 怎么看运行过程...嗯添加一个正在运行 页面还是窗体? )
            日志查看
            性能监控
            还没想好还有什么内容
    -->

    <Window.Resources>

    </Window.Resources>
    <Grid RowDefinitions="auto,1*,auto" ColumnDefinitions="1*,auto">
        <!-- 0,0 顶栏 放 (模块 提供长按左键移动到工作流中) 可固定,或者可以折叠,或者可以拖动改变宽度大小
            按类型分区,展开缩放,可以考虑添加搜索功能,搜索模块类型,搜索模块名称
            还没想好还有什么内容
        -->
        <!-- 1,1 侧边 放(工作流+模块参数填写)可固定,或者可以折叠,或者可以拖动改变宽度大小 自动隐藏
            工作流区域(历史打开,查找,分组,参数,[鼠标拖拽其他工作流到工作流区域,可以链接此工作流.]

                1.提供工作流的创建,保存,打开,运行,调试等功能
                2.提供工作流的编辑功能,可以添加模块,删除模块,移动模块,连接模块等功能
                3.提供工作流的调试功能,可以查看工作流的运行状态,查看工作流的运行日志等功能

                还没想好还有什么内容
            )
            模块参数填写区域(模块参数填写,搜索复制参数,模块参数保存)
                1.提供模块的参数填写功能,可以填写模块的参数,可以验证模块的参数,可以保存模块的参数等功能
                2.提供模块的参数验证功能,可以验证模块的参数是否正确,可以提示用户模块的参数是否正确等功能
                3.提供模块的参数保存功能,可以保存模块的参数,可以加载模块的参数等功能
                还没想好还有什么内容
            )
           -->
        <!-- 1,0  中间 放工作流区域 (整体缩放, 思维导图类型, 流程箭头[异步虚线,同步实线])
                选择 模块后 侧边栏显示模块参数填写区域
                鼠标 右键菜单栏 添加模块
                鼠标选中模块后右键复制添加模块
        -->
        <!-- 2,0 底部 放日志区域,工作流运行状态,模块个数等 自动隐藏-->


        <!-- 下面是实现示例 -->

        <Grid Grid.Row="0" Grid.Column="0">
            <!-- 顶栏 模块 区域 拖拽模块,模块类型分类,扩展-->
        </Grid>
        <Grid Grid.RowSpan="2"  Grid.Column="1"   RowDefinitions="1*,auto">
            <Grid Grid.Row="0">
                <!-- 侧边栏 工作流区域 -->
            </Grid>
            <Grid Grid.Row="1">
                <!-- 侧边栏 模块参数填写区域 -->
            </Grid>
        </Grid>
        <TabControl Grid.Row="1" Grid.Column="0">
            <TabItem Header="工作流1">
                <!-- 工作流工作区域 -->
                <Grid>
                    <!-- 整体缩放, 思维导图类型, 流程箭头[异步虚线,同步实线]  -->
                </Grid>
            </TabItem>
            <TabItem Header="+" />
        </TabControl>
        <Grid Grid.Row="2"
              Grid.Column="0"
              Grid.ColumnSpan="2">
            <!-- 底部 日志区域,工作流运行状态,模块个数等 -->
        </Grid>
    </Grid>
</Window>
